monolog:
    handlers:
        custom:
            type_stream:
                path: /tmp/symfony.log
                bubble: true
                level: ERROR
        main:
            type_buffer:
                level: INFO
                handler: nested
        nested:
            type_stream: ~
