monolog:
    handlers:
        custom:
            type_stream:
                path: /tmp/symfony.log
                bubble: false
                level: ERROR
        main:
            type_fingers_crossed:
                action_level: ERROR
                handler: nested
        nested:
            type_stream: ~
