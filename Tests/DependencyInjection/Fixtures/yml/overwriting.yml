imports:
    - { resource: overwriting_import.yml }

monolog:
    handlers:
        custom:
            type_stream:
                path: /tmp/symfony.log
                bubble: true
                level: WARNING
